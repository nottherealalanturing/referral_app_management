const generate = document.querySelector = (".create-link");
const createModal = document.querySelector = (".createUser-modal-bg");

generate.addEventListener ('click' , function(){
    createModal.classList.add('createUser-modal-active');
});